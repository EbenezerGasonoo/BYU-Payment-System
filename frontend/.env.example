# Frontend Environment Variables

# API URL Configuration
# When running locally with Vite dev server, use the proxy (default: /api)
# When deployed or using direct connection, set to full URL (e.g., http://localhost:3000/api)
# VITE_API_URL=/api

# Leave empty to use default proxy configuration

